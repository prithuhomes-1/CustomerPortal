# Assets

Use this folder for static media references used by frontend pages.

- `images/`: logos, card images, icons.
- `videos/`: hero/background videos.

Update media paths in `frontend/content.json`.
